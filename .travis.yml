language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-dashboard' ]; then cd .. && eval "mv $currentfolder generator-dashboard" && cd generator-dashboard; fi

